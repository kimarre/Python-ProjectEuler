#sum the values of the fibbonacci sequence that are even
#while var1 isn't larger than 4 million
#var1 = the current value, var2 = last value
# if val1 is even, add it to sum,
# else, store var2 in temp and add the two.
# then set` result to var2 
